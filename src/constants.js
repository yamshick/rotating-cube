export const W = 600, H = 600;
export const MODEL_MIN_X = -2, MODEL_MAX_X = 2;
export const MODEL_MIN_Y = -2, MODEL_MAX_Y = 2;

const canvas = document.getElementById('canvas');
export const CONTEXT = canvas.getContext('2d');
